void dfs(type &input, type &path, type &result, int cur){
	if(/*invalid input*/) return 0;
	if(cur==input.size()){
		if(gap==0){
			//put path to result;
		}
	}
	if(//cutting edge) return;;

	for(){
		//modify path;
		dfs(input,step+1,result);
		//return path;
	}
}